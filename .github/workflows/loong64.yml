name: loong64
on:
  push:
    branches:
      - loong64-*
    tags:
      - release-loong64-*
  pull_request:
    branches:
      - loong64-*

env:
  NAMESPACE: ${{ vars.NAMESPACE || 'kubernetesloong64' }}

jobs:
  build:
    runs-on: ubuntu-latest
    container: debian:13
    outputs:
      DOCKER_TAG: ${{ steps.extract.outputs.DOCKER_TAG }}
      GITLAB_RUNNER_BRANCH: ${{ steps.extract.outputs.GITLAB_RUNNER_BRANCH }}
      IS_STABLE: ${{ steps.extract.outputs.IS_STABLE }}
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - name: Print branch name
        run: |
          if [ "${{ github.event_name }}" = "pull_request" ]; then
            echo "${{ github.base_ref }} (PR target)"
          else
            echo ${{ github.ref_name }}
          fi
      - name: Extract gitlab-runner branch
        id: extract
        run: |
          if [ "${{ github.event_name }}" = "pull_request" ]; then
            REF=${{ github.base_ref }}
          else
            REF=${{ github.ref_name }}
          fi
          echo "REF=$REF"
          RAW_VERSION=$(echo $REF | awk -F'loong64-' '{print $2}')
          echo "RAW_VERSION=$RAW_VERSION"
          echo "RAW_VERSION=$RAW_VERSION" >> ${GITHUB_ENV}
          echo "RAW_VERSION=$RAW_VERSION" >> ${GITHUB_OUTPUT}
          GITLAB_RUNNER_BRANCH=$(echo $RAW_VERSION | awk -F'+' '{print $1}')
          echo "GITLAB_RUNNER_BRANCH=$GITLAB_RUNNER_BRANCH"
          echo "GITLAB_RUNNER_BRANCH=$GITLAB_RUNNER_BRANCH" >> ${GITHUB_ENV}
          echo "GITLAB_RUNNER_BRANCH=$GITLAB_RUNNER_BRANCH" >> ${GITHUB_OUTPUT}
          DOCKER_TAG=$(echo $RAW_VERSION | sed 's/+/-/g')
          echo "DOCKER_TAG=$DOCKER_TAG"
          echo "DOCKER_TAG=$DOCKER_TAG" >> ${GITHUB_ENV}
          echo "DOCKER_TAG=$DOCKER_TAG" >> ${GITHUB_OUTPUT}
          RELEASE=$(echo $RAW_VERSION | awk -F'+' '{print $2}')
          RELEASE=${RELEASE:-alpha.${{ github.run_number }}}
          echo "RELEASE=$RELEASE"
          echo "RELEASE=$RELEASE" >> ${GITHUB_ENV}
          echo "RELEASE=$RELEASE" >> ${GITHUB_OUTPUT}
          if echo "$RAW_VERSION" | grep -qE '[+].*(alpha|beta|rc)'; then
            IS_STABLE=false
          else
            IS_STABLE=true
          fi
          echo "IS_STABLE=$IS_STABLE"
          echo "IS_STABLE=$IS_STABLE" >> ${GITHUB_OUTPUT}
      - name: Install build tools
        run: |
          apt update
          apt install -y git make golang file jq
      - name: Clone gitlab-runner
        run: |
          git clone --branch ${{ env.GITLAB_RUNNER_BRANCH }} --depth 1 https://gitlab.com/gitlab-org/gitlab-runner.git gitlab-runner
      - name: ls
        run: ls -lah
      - name: Install Docker CLI
        run: |
          apt remove $(dpkg --get-selections docker.io docker-compose docker-doc podman-docker containerd runc | cut -f1)
          apt update
          apt install -y ca-certificates curl
          install -m 0755 -d /etc/apt/keyrings
          curl -fsSL https://download.docker.com/linux/debian/gpg -o /etc/apt/keyrings/docker.asc
          chmod a+r /etc/apt/keyrings/docker.asc
          # Add the repository to Apt sources:
          tee /etc/apt/sources.list.d/docker.sources <<EOF
          Types: deb
          URIs: https://download.docker.com/linux/debian
          Suites: $(. /etc/os-release && echo "$VERSION_CODENAME")
          Components: stable
          Architectures: $(dpkg --print-architecture)
          Signed-By: /etc/apt/keyrings/docker.asc
          EOF
          apt update
          apt install -y docker-ce-cli
      - name: Verify Docker installation
        run: |
          ls -la /run/docker.sock 2>/dev/null || echo "/run/docker.sock not found"
          echo "DOCKER_HOST=$DOCKER_HOST"
          docker info
      #- name: Apply patches
      #  working-directory: gitlab-runner
      #  run: git apply ../*.patch
      - name: Build gitlab-runner-helper
        working-directory: gitlab-runner
        env:
          GOOS: linux
          GOARCH: loong64
          ARCH_TAG: loongarch64
          BASE_IMAGE: kubernetesloong64/runner-tools-base-images-loong64:v0.0.48-debian
        run: |
          echo "=== Build gitlab-runner-helper ==="
          echo "GOOS=$GOOS GOARCH=$GOARCH ARCH_TAG=$ARCH_TAG BASE_IMAGE=$BASE_IMAGE"
          IMAGE_REF=${{ env.NAMESPACE }}/gitlab-runner-helper:$ARCH_TAG-${{ env.DOCKER_TAG }}
          IMAGE_REF_LOONG64=${{ env.NAMESPACE }}/gitlab-runner-helper-loong64:$ARCH_TAG-${{ env.DOCKER_TAG }}
          echo "Target: $IMAGE_REF"
          echo "IMAGE_REF=$IMAGE_REF"
          echo "IMAGE_REF=$IMAGE_REF" >> ${GITHUB_ENV}
          echo "Target: $IMAGE_REF_LOONG64"
          echo "IMAGE_REF_LOONG64=$IMAGE_REF_LOONG64"
          echo "IMAGE_REF_LOONG64=$IMAGE_REF_LOONG64" >> ${GITHUB_ENV}
          make helper-bin-linux
          echo "=== Binary output ==="
          find out -type f | xargs ls -lh
          echo "=== Docker images before build ==="
          docker images
          docker build \
              --build-arg BASE_IMAGE=$BASE_IMAGE \
              --build-arg TARGETOS=$GOOS \
              --build-arg TARGETARCH=$GOARCH \
              --build-context binary_dir=out/binaries/gitlab-runner-helper \
              -f dockerfiles/runner-helper/Dockerfile \
              -t $IMAGE_REF \
              -t $IMAGE_REF_LOONG64 \
              .
          echo "=== Docker images after build ==="
          docker images
          TAR_FILE=gitlab-runner-helper-loong64-${{ env.DOCKER_TAG }}.tar
          echo "=== Saving $IMAGE_REF and $IMAGE_REF_LOONG64 -> $TAR_FILE ==="
          docker save -o "$TAR_FILE" $IMAGE_REF $IMAGE_REF_LOONG64
          echo "=== Image tarball ==="
          ls -lha "$TAR_FILE"
          echo "=== Manifest ==="
          echo "--- repositories ---"
          tar -xOf "$TAR_FILE" repositories
          echo "--- manifest.json ---"
          tar -xOf "$TAR_FILE" manifest.json | jq
          echo "--- config ---"
          tar -xOf "$TAR_FILE" $(tar -xOf "$TAR_FILE" manifest.json | jq -r '.[0].Config') | jq
          echo "--- os, architecture ---"
          tar -xOf "$TAR_FILE" $(tar -xOf "$TAR_FILE" manifest.json | jq -r '.[0].Config') | jq '{os, architecture}'
      - name: Print binary SHA-256 checksums
        working-directory: gitlab-runner
        run: |
          find out -type f | xargs sha256sum
      - name: Inspect binary file types
        working-directory: gitlab-runner
        run: |
          echo "=== bin ==="
          file out/binaries/gitlab-runner-helper/*
      - name: Upload gitlab-runner-helper binaries
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: gitlab-runner-helper-loong64-binaries
          path: gitlab-runner/out/binaries/gitlab-runner-helper/gitlab-runner-helper.linux-loong64
      - name: Upload gitlab-runner-helper image
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: gitlab-runner-helper-loong64-image
          path: gitlab-runner/gitlab-runner-helper-loong64-${{ env.DOCKER_TAG }}.tar

  release:
    if: startsWith(github.ref, 'refs/tags/')
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    env:
      BASE_URL: https://github.com/${{ github.repository }}/releases/download/${{ github.ref_name }}
      DOCKER_TAG: ${{ needs.build.outputs.DOCKER_TAG }}
      GITLAB_RUNNER_BRANCH: ${{ needs.build.outputs.GITLAB_RUNNER_BRANCH }}
      IS_STABLE: ${{ needs.build.outputs.IS_STABLE }}
    steps:
      - name: Download container images
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          pattern: gitlab-runner-helper-loong64-*
          path: loong64-artifacts
          merge-multiple: true
      - name: List downloaded files
        run: find . -type f
      - name: Login to Docker
        uses: docker/login-action@650006c6eb7dba73a995cc03b0b2d7f5ca915bee # v4.2.0
        with:
          username: ${{ vars.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
      - name: Load and push Docker images
        run: |
          echo "=== Loading images ==="
          for tar in loong64-artifacts/gitlab-runner-helper-loong64-*.tar; do
            echo "=== Loading $tar ==="
            docker load -i "$tar"
          done
          echo "=== After load ==="
          docker images

          ARCH_TAG=loongarch64

          IMAGE_REF=${{ env.NAMESPACE }}/gitlab-runner-helper:$ARCH_TAG-${{ env.DOCKER_TAG }}
          IMAGE_REF_LOONG64=${{ env.NAMESPACE }}/gitlab-runner-helper-loong64:$ARCH_TAG-${{ env.DOCKER_TAG }}
          GITLAB_RUNNER_BRANCH_IMAGE_REF=${{ env.NAMESPACE }}/gitlab-runner-helper:$ARCH_TAG-${{ env.GITLAB_RUNNER_BRANCH }}
          GITLAB_RUNNER_BRANCH_IMAGE_REF_LOONG64=${{ env.NAMESPACE }}/gitlab-runner-helper-loong64:$ARCH_TAG-${{ env.GITLAB_RUNNER_BRANCH }}

          echo "=== Pushing $IMAGE_REF ==="
          docker push $IMAGE_REF
          echo "=== Pushing $IMAGE_REF_LOONG64 ==="
          docker push $IMAGE_REF_LOONG64

          echo "=== Tagging and pushing branch tags ==="
          docker tag $IMAGE_REF $GITLAB_RUNNER_BRANCH_IMAGE_REF
          docker push $GITLAB_RUNNER_BRANCH_IMAGE_REF
          docker tag $IMAGE_REF_LOONG64 $GITLAB_RUNNER_BRANCH_IMAGE_REF_LOONG64
          docker push $GITLAB_RUNNER_BRANCH_IMAGE_REF_LOONG64

          echo "=== Push complete ==="
      - name: Import GPG key
        run: |
          echo "${{ secrets.GPG_PRIVATE_KEY_BASE64 }}" | base64 -d | gpg --batch --import > /dev/null
          echo "$(gpg --list-secret-keys --keyid-format LONG | grep -Eo '[A-F0-9]{40}' | head -1):6:" | gpg --import-ownertrust
      - name: Sign artifacts
        run: |
          find loong64-artifacts -type f | while read -r file; do
            gpg --detach-sign --armor "$file"
            echo "Signed: $file"
            gpg --verify "$file".asc "$file"
          done
      - name: Generate release body
        run: |
          cat > release-body.md <<'BODYEOF'
          ## Artifacts

          | File                                                     | Download                                                                                                                             |
          |----------------------------------------------------------|--------------------------------------------------------------------------------------------------------------------------------------|
          | `gitlab-runner-helper-loong64-${{ env.DOCKER_TAG }}.tar` | [gitlab-runner-helper-loong64-${{ env.DOCKER_TAG }}.tar](${{ env.BASE_URL }}/gitlab-runner-helper-loong64-${{ env.DOCKER_TAG }}.tar) |
          | `gitlab-runner-helper.linux-loong64`                     | [gitlab-runner-helper.linux-loong64](${{ env.BASE_URL }}/gitlab-runner-helper.linux-loong64)                                         |

          Each file has a corresponding `.asc` detached GPG signature.

          ## Docker image

          ```
          ${{ env.NAMESPACE }}/gitlab-runner-helper:loongarch64-${{ env.DOCKER_TAG }}
          ${{ env.NAMESPACE }}/gitlab-runner-helper-loong64:loongarch64-${{ env.DOCKER_TAG }}
          ```

          Download and load the image:

          ```shell
          curl -LO ${{ env.BASE_URL }}/gitlab-runner-helper-loong64-${{ env.DOCKER_TAG }}.tar
          docker load -i gitlab-runner-helper-loong64-${{ env.DOCKER_TAG }}.tar
          ```

          ## Verifying releases

          ```shell
          gpg --keyserver keys.openpgp.org --recv-keys FCF8724722CCBF9F51B1FBE376532BE7E3013105
          echo "FCF8724722CCBF9F51B1FBE376532BE7E3013105:6:" | gpg --import-ownertrust
          gpg --verify <file>.asc <file>
          ```
          BODYEOF
      - name: Create Release
        uses: softprops/action-gh-release@b4309332981a82ec1c5618f44dd2e27cc8bfbfda # v3.0.0
        with:
          name: ${{ github.ref_name }}
          body_path: release-body.md
          prerelease: ${{ env.IS_STABLE == 'false' }}
          make_latest: ${{ env.IS_STABLE == 'true' && 'legacy' || 'false' }}
          files: |
            loong64-artifacts/**

  sync-registry:
    if: startsWith(github.ref, 'refs/tags/') && vars.SYNC_REGISTRY != ''
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: read
    env:
      DOCKER_TAG: ${{ needs.build.outputs.DOCKER_TAG }}
      GITLAB_RUNNER_BRANCH: ${{ needs.build.outputs.GITLAB_RUNNER_BRANCH }}
      IS_STABLE: ${{ needs.build.outputs.IS_STABLE }}
    steps:
      - name: Download container images
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          pattern: gitlab-runner-helper-loong64-image
          path: loong64-artifacts
          merge-multiple: true
      - name: Load Docker images
        run: |
          for tar in loong64-artifacts/gitlab-runner-helper-loong64-*.tar; do
            echo "=== Loading $tar ==="
            docker load -i "$tar"
          done
          echo "=== After load ==="
          docker images
      - name: Login to sync container registry
        uses: docker/login-action@650006c6eb7dba73a995cc03b0b2d7f5ca915bee # v4.2.0
        with:
          registry: ${{ vars.SYNC_REGISTRY }}
          username: ${{ vars.SYNC_REGISTRY_USERNAME }}
          password: ${{ secrets.SYNC_REGISTRY_PASSWORD }}
      - name: Sync Docker images to sync container registry
        run: |
          ARCH_TAG=loongarch64

          IMAGE_REF=${{ env.NAMESPACE }}/gitlab-runner-helper:$ARCH_TAG-${{ env.DOCKER_TAG }}
          IMAGE_REF_LOONG64=${{ env.NAMESPACE }}/gitlab-runner-helper-loong64:$ARCH_TAG-${{ env.DOCKER_TAG }}

          SYNC_IMAGE_REF=${{ vars.SYNC_REGISTRY }}/${{ env.NAMESPACE }}/gitlab-runner-helper:$ARCH_TAG-${{ env.DOCKER_TAG }}
          SYNC_IMAGE_REF_LOONG64=${{ vars.SYNC_REGISTRY }}/${{ env.NAMESPACE }}/gitlab-runner-helper-loong64:$ARCH_TAG-${{ env.DOCKER_TAG }}

          SYNC_GITLAB_RUNNER_BRANCH_IMAGE_REF=${{ vars.SYNC_REGISTRY }}/${{ env.NAMESPACE }}/gitlab-runner-helper:$ARCH_TAG-${{ env.GITLAB_RUNNER_BRANCH }}
          SYNC_GITLAB_RUNNER_BRANCH_IMAGE_REF_LOONG64=${{ vars.SYNC_REGISTRY }}/${{ env.NAMESPACE }}/gitlab-runner-helper-loong64:$ARCH_TAG-${{ env.GITLAB_RUNNER_BRANCH }}

          echo "=== Syncing $IMAGE_REF -> $SYNC_IMAGE_REF ==="
          docker tag $IMAGE_REF $SYNC_IMAGE_REF
          docker push $SYNC_IMAGE_REF

          echo "=== Syncing $IMAGE_REF_LOONG64 -> $SYNC_IMAGE_REF_LOONG64 ==="
          docker tag $IMAGE_REF_LOONG64 $SYNC_IMAGE_REF_LOONG64
          docker push $SYNC_IMAGE_REF_LOONG64

          echo "=== Syncing branch tags ==="
          docker tag $IMAGE_REF $SYNC_GITLAB_RUNNER_BRANCH_IMAGE_REF
          docker push $SYNC_GITLAB_RUNNER_BRANCH_IMAGE_REF
          docker tag $IMAGE_REF_LOONG64 $SYNC_GITLAB_RUNNER_BRANCH_IMAGE_REF_LOONG64
          docker push $SYNC_GITLAB_RUNNER_BRANCH_IMAGE_REF_LOONG64

          echo "=== Registry sync complete ==="
